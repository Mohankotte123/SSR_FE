# Backend API (SSR_BE) — used by Next rewrites + server-side fetches
NEXT_PUBLIC_API_URL=https://ssr-be.onrender.com

# WhatsApp enquire number (E.164 without +) — used by plot drawer deep-link
NEXT_PUBLIC_WHATSAPP_NUMBER=919849105449

# Optional fallback admin Bearer token (prefer /admin/login)
NEXT_PUBLIC_ADMIN_TOKEN=
